<?php
return [
  'base_url' => '',
  'default_language' => 'en',
  // Set to false in production so PHP errors are logged, not shown to visitors.
  'debug' => false,
  'session' => [
    // Set to true once the site is served over HTTPS.
    'cookie_secure' => false,
    'cookie_httponly' => true,
    'cookie_samesite' => 'Lax',
  ],
];

